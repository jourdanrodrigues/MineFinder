import { cn } from '@/utils';

export const buttonClassName = cn(
  'px-4 py-2 outline-none rounded-lg border border-contrast bg-transparent transition-[filter] hover:brightness-90 dark:hover:brightness-125 dark:border-contrast-dark dark:text-contrast-dark',
);
